function accessEndpoint() {
    // Obtener la URL del endpoint deseado
    var endpointURL = "https://www.ejemplo.com/endpoint";
  
    // Realizar alguna acción con la URL, por ejemplo, redireccionar a la URL
    window.location.href = endpointURL;
  }
  